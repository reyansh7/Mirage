worker_processes auto;
events { worker_connections 1024; }
http {
  upstream portal {
    server 10.0.5.22:8080;
  }
  server {
    listen 80;
    server_name portal.acme-tech.internal;
    location / {
      proxy_pass http://portal;
    }
  }
}
